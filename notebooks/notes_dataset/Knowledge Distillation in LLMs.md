They used the logits from the biggers models to train the smaller models, because by using those, the smaller models can learn to mimic the behavior of biggers models by learning from the unormalized outputs (logits). These logits provide insight into how the teacher model ranks alternative token predictions, encoding richer information about the language model's understanding of context and token relationships.